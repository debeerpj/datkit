# Claude outputs — what is current

Drafts for review. Nothing here is on the import path.

| File | Status |
|---|---|
| `binning_v2.py` | **Current** profiling module. |
| `cleaning_changes.py` | **Current** draft of the cleaning additions. |
| `binning.py`, `binning-1.py` | Superseded — delete. |
| `bin_numeric.py`, `bin_numeric-1.py`, `bin_numeric-2.py`, `bin_categorical.py` | Superseded — delete. |

The duplicates exist because whole files were sent into the chat and downloaded
repeatedly. From now on edits happen in place on a branch, per `CLAUDE.md`, so
this folder should stop accumulating versions.
